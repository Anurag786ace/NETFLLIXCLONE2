
// Add this JavaScript code to your existing script or in a separate file

/*document.addEventListener('DOMContentLoaded', function () {
    const faqItems = document.querySelectorAll('.faq-item');

    faqItems.forEach(function (item) {
        const toggleIcon = item.querySelector('.toggle-icon');
        const answer = item.querySelector('.answer');

        item.addEventListener('click', function () {
            // Toggle the 'show' class on the answer element
            answer.classList.toggle('show');

            // Change the text of the toggle icon based on visibility
            toggleIcon.textContent = answer.classList.contains('show') ? '-' : '+';
        });
    });
});

// Add this JavaScript code to your existing script or in a separate file

document.addEventListener('DOMContentLoaded', function () {
    const faqItems = document.querySelectorAll('.faq-item');

    faqItems.forEach(function (item) {
        const toggleIcon = item.querySelector('.toggle-icon');
        const answer = item.querySelector('.answer');

        item.addEventListener('click', function () {
            // Toggle the 'show-answer' class on the FAQ item
            item.classList.toggle('show-answer');
        });
    });
});*/

